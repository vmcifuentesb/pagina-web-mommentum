---
// Fullscreen Photographic Lightbox Modal with Touch Swipe & Next / Prev Navigation
---

<div id="global-photo-modal" class="fixed inset-0 z-[999990] bg-[#07080B]/98 backdrop-blur-2xl hidden items-center justify-center p-3 sm:p-6 md:p-10 select-none touch-none">
  
  <!-- Close Button (Top Right) -->
  <button id="close-lightbox-btn" class="absolute top-4 right-4 sm:top-6 sm:right-6 z-50 w-11 h-11 sm:w-12 sm:h-12 rounded-full bg-[#14161F]/90 border border-white/20 hover:border-white flex items-center justify-center text-white hover:bg-white hover:text-black transition-all font-mono text-sm shadow-xl active:scale-95" aria-label="Cerrar [ESC]" data-cursor="pointer">
    ✕
  </button>

  <!-- Left Arrow Button (Previous Photo) -->
  <button 
    id="lightbox-prev-btn" 
    class="absolute left-2 sm:left-6 top-1/2 -translate-y-1/2 z-50 w-10 sm:w-14 h-20 sm:h-24 rounded-lg bg-[#14161F]/80 hover:bg-white text-white hover:text-black backdrop-blur-md border border-white/15 flex items-center justify-center transition-all group shadow-xl active:scale-95"
    aria-label="Foto Anterior [←]"
    data-cursor="pointer"
  >
    <svg class="w-6 sm:w-8 h-6 sm:h-8 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
    </svg>
  </button>

  <!-- Right Arrow Button (Next Photo) -->
  <button 
    id="lightbox-next-btn" 
    class="absolute right-2 sm:right-6 top-1/2 -translate-y-1/2 z-50 w-10 sm:w-14 h-20 sm:h-24 rounded-lg bg-[#14161F]/80 hover:bg-white text-white hover:text-black backdrop-blur-md border border-white/15 flex items-center justify-center transition-all group shadow-xl active:scale-95"
    aria-label="Foto Siguiente [→]"
    data-cursor="pointer"
  >
    <svg class="w-6 sm:w-8 h-6 sm:h-8 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
    </svg>
  </button>

  <!-- Main Modal Container Full Width -->
  <div class="relative w-full h-full max-h-[92vh] flex flex-col md:flex-row items-center justify-between gap-4 md:gap-8 px-4 sm:px-14 md:px-16 overflow-y-auto md:overflow-hidden">
    
    <!-- Large Photo Frame -->
    <div id="lightbox-touch-area" class="relative flex-1 w-full h-[60vh] md:h-full flex items-center justify-center overflow-hidden">
      <img 
        id="lightbox-img" 
        src="" 
        alt="" 
        class="max-w-full max-h-full object-contain filter contrast-105 shadow-2xl rounded-lg transition-opacity duration-200" 
      />
    </div>

    <!-- Curatorial & Technical Sidebar Full Width Info -->
    <div class="w-full md:w-80 flex-shrink-0 font-mono text-xs text-silver space-y-3 sm:space-y-4 border-t md:border-t-0 md:border-l border-white/10 pt-3 md:pt-0 md:pl-8 bg-[#14161F]/70 p-4 md:bg-transparent rounded-xl">
      <div>
        <div class="flex items-center justify-between font-mono text-[9px] sm:text-[10px] text-graphite uppercase tracking-widest mb-1">
          <span>// FICHA TÉCNICA</span>
          <span id="lightbox-counter" class="text-white font-bold">FOTO 01 / 01</span>
        </div>
        <h3 id="lightbox-title" class="font-condensed text-2xl sm:text-3xl font-bold uppercase text-white leading-tight">
          -
        </h3>
        <p id="lightbox-copy" class="font-body text-[11px] sm:text-xs text-silver/90 mt-1">
          -
        </p>
      </div>

      <div class="space-y-1.5 sm:space-y-2 border-t border-white/10 pt-3 text-[11px] sm:text-xs">
        <div class="flex justify-between">
          <span class="text-graphite">ÁLBUM:</span>
          <span id="lightbox-album" class="text-white font-bold truncate max-w-[140px]">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-graphite">OBTURACIÓN:</span>
          <span id="lightbox-shutter" class="text-white font-bold">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-graphite">APERTURA:</span>
          <span id="lightbox-aperture" class="text-white font-bold">-</span>
        </div>
        <div class="flex justify-between">
          <span class="text-graphite">SENSIBILIDAD:</span>
          <span id="lightbox-iso" class="text-white font-bold">-</span>
        </div>
      </div>

      <div class="pt-2 border-t border-white/10 text-[9px] sm:text-[10px] text-graphite flex items-center justify-between">
        <span>DESLIZA O TECLAS ← / →</span>
        <span>ESC: SALIR</span>
      </div>
    </div>

  </div>

</div>

<script>
  const modal = document.getElementById('global-photo-modal');
  const closeBtn = document.getElementById('close-lightbox-btn');
  const prevBtn = document.getElementById('lightbox-prev-btn');
  const nextBtn = document.getElementById('lightbox-next-btn');
  const touchArea = document.getElementById('lightbox-touch-area');

  const imgElem = document.getElementById('lightbox-img') as HTMLImageElement;
  const counterElem = document.getElementById('lightbox-counter');
  const titleElem = document.getElementById('lightbox-title');
  const copyElem = document.getElementById('lightbox-copy');
  const albumElem = document.getElementById('lightbox-album');
  const shutterElem = document.getElementById('lightbox-shutter');
  const apertureElem = document.getElementById('lightbox-aperture');
  const isoElem = document.getElementById('lightbox-iso');

  let currentPhotoList: any[] = [];
  let currentIndex = 0;

  const renderCurrentPhoto = () => {
    if (currentPhotoList.length === 0) return;
    const data = currentPhotoList[currentIndex];
    if (!data) return;

    if (imgElem) {
      imgElem.style.opacity = '0.3';
      imgElem.src = data.url || '';
      imgElem.alt = data.title || '';
      setTimeout(() => {
        imgElem.style.opacity = '1';
      }, 50);
    }

    if (counterElem) counterElem.textContent = `FOTO 0${currentIndex + 1} / 0${currentPhotoList.length}`;
    if (titleElem) titleElem.textContent = data.title || 'MOMMENTUM';
    if (copyElem) copyElem.textContent = data.copy || '';
    if (albumElem) albumElem.textContent = data.album || 'MOMMENTUM';
    if (shutterElem) shutterElem.textContent = data.shutter || '1/250s';
    if (apertureElem) apertureElem.textContent = data.aperture || 'f/2.8';
    if (isoElem) isoElem.textContent = data.iso || 'ISO 100';
  };

  const showNextPhoto = () => {
    if (currentPhotoList.length <= 1) return;
    currentIndex = (currentIndex + 1) % currentPhotoList.length;
    renderCurrentPhoto();
  };

  const showPrevPhoto = () => {
    if (currentPhotoList.length <= 1) return;
    currentIndex = (currentIndex - 1 + currentPhotoList.length) % currentPhotoList.length;
    renderCurrentPhoto();
  };

  const openModal = (targetUrl: string) => {
    if (!modal) return;

    const triggers = Array.from(document.querySelectorAll('[data-lightbox-photo]'));
    currentPhotoList = triggers.map((el) => ({
      url: el.getAttribute('data-url') || (el as HTMLImageElement).src || '',
      title: el.getAttribute('data-title') || '',
      copy: el.getAttribute('data-copy') || '',
      album: el.getAttribute('data-album') || '',
      shutter: el.getAttribute('data-shutter') || '',
      aperture: el.getAttribute('data-aperture') || '',
      iso: el.getAttribute('data-iso') || '',
      dim: el.getAttribute('data-dim') || '',
    })).filter(p => p.url !== '');

    const foundIdx = currentPhotoList.findIndex(p => p.url === targetUrl);
    currentIndex = foundIdx !== -1 ? foundIdx : 0;

    renderCurrentPhoto();

    modal.classList.remove('hidden');
    modal.classList.add('flex');
    document.body.style.overflow = 'hidden';
  };

  const closeModal = () => {
    if (!modal) return;
    modal.classList.add('hidden');
    modal.classList.remove('flex');
    document.body.style.overflow = 'auto';
  };

  if (closeBtn) closeBtn.addEventListener('click', closeModal);
  if (prevBtn) prevBtn.addEventListener('click', showPrevPhoto);
  if (nextBtn) nextBtn.addEventListener('click', showNextPhoto);

  if (modal) {
    modal.addEventListener('click', (e) => {
      if (e.target === modal) closeModal();
    });
  }

  // Touch Swipe for Mobile Lightbox
  let touchStartX = 0;
  let touchDistX = 0;

  modal?.addEventListener('touchstart', (e: TouchEvent) => {
    if (e.touches.length === 1) {
      touchStartX = e.touches[0].clientX;
      touchDistX = 0;
    }
  }, { passive: true });

  modal?.addEventListener('touchmove', (e: TouchEvent) => {
    if (e.touches.length === 1) {
      touchDistX = e.touches[0].clientX - touchStartX;
    }
  }, { passive: true });

  modal?.addEventListener('touchend', () => {
    if (Math.abs(touchDistX) > 45) {
      if (touchDistX < 0) {
        showNextPhoto();
      } else {
        showPrevPhoto();
      }
    }
  });

  // Keyboard navigation for Modal
  window.addEventListener('keydown', (e) => {
    if (modal && !modal.classList.contains('hidden')) {
      if (e.key === 'Escape') closeModal();
      if (e.key === 'ArrowRight' || e.key === 'd' || e.key === 'D') showNextPhoto();
      if (e.key === 'ArrowLeft' || e.key === 'a' || e.key === 'A') showPrevPhoto();
    }
  });

  // Attach click listener to all trigger elements
  const attachTriggers = () => {
    document.querySelectorAll('[data-lightbox-photo]').forEach((el) => {
      el.addEventListener('click', (e) => {
        e.preventDefault();
        const url = el.getAttribute('data-url') || (el as HTMLImageElement).src || '';
        if (url) openModal(url);
      });
    });
  };

  attachTriggers();
  document.addEventListener('astro:page-load', attachTriggers);
</script>
